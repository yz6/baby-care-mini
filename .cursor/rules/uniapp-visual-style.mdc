---
description: 乡小育视觉与样式统一规范（简约专业+柔和乡村教育风）
alwaysApply: true
---

# Xiang Xiaoyu Visual Style

## 视觉定位
- 风格固定为 `简约专业`，强调清晰信息层级与低认知负担。
- 主色调使用 `浅绿 + 米白 + 浅灰`，避免高饱和撞色和复杂装饰纹理。
- 页面应服务教师长时间操作：减少视觉噪音、保证阅读舒适、控件易点击。

## 颜色与令牌
- 颜色必须通过 `uni.scss` 设计令牌使用，不在页面内硬编码品牌色。
- 建议基础色板：`$color-primary: #2F8A5B`、`$color-bg-page: #F7F8FA`、`$color-bg-card: #FFFFFF`、`$color-text-secondary: #646A73`。
- 状态色（成功/警告/错误）饱和度控制在中低区间，避免刺激性强的纯色。

## 布局与组件
- 页面采用卡片化分区，优先使用留白和分组表达层次，不依赖重边框。
- 列表项、按钮、输入框点击热区不小于 `88rpx` 高度（或等效可点击面积）。
- 统一圆角和阴影强度：轻阴影、小圆角，避免拟物化效果。

## 图标与插画
- 图标统一线性/简面风，围绕 `教案`、`评估表`、`入户导航` 三类语义。
- 图标视觉权重低于正文信息，不使用过度写实或多色渐变图标。
- 可加入简约乡村元素（田野/小屋/路径）作为点缀，不得喧宾夺主。

## 可读性与无障碍
- 正文与背景对比度满足可读性，浅色背景上避免过浅文本。
- 同一页面字体层级控制在 3 级以内，保证扫描效率。
- 关键信息不只靠颜色区分，需配合文案或图标提示状态。
