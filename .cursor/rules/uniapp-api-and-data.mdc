---
description: uni-app 接口请求、数据建模与错误处理规范
globs: **/*.{ts,js,vue}
alwaysApply: false
---

# API And Data Rules

## 接口调用
- 网络请求统一经 `api/` 或 `services/` 层封装，页面中不直接散落 `uni.request`。
- 每个接口函数应返回统一结构（如 `{ data, error }` 或显式抛错），避免多种返回形态混用。
- 请求超时、网络异常、业务失败码都要处理，并给用户明确反馈。

## 数据处理
- 接口原始字段到页面展示字段之间增加映射层，避免模板直接依赖后端命名。
- 时间、数字、状态值格式化逻辑抽离为工具函数，避免重复复制。
- 对可空字段做默认值兜底，防止页面出现 `undefined/null` 展示。
- 领域对象命名统一：`childProfile`、`familyProfile`、`homeVisitRecord`、`activityRecord`、`teachingResource`。

## 状态管理
- 跨页面共享状态放入统一 store（如 Pinia），页面局部状态留在组件内部。
- 异步流程维护 loading/error 状态，不用隐式布尔变量拼凑流程。
- 列表与详情状态分离，优先命名为 `xxxListState` 与 `xxxDetailState`，避免混用字段。
