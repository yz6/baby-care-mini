---
description: uni-app 页面与组件（Vue SFC）开发规范
globs: **/*.vue
alwaysApply: false
---

# uni-app Vue SFC

## 组件组织
- 页面使用页面级容器组件，复用片段抽为 `components` 子组件。
- `props`、`emits` 必须显式声明，避免隐式事件和魔法字符串。
- 生命周期优先使用 `onLoad`、`onShow`、`onPullDownRefresh` 等 uni-app 页面钩子。

## 模板与交互
- 模板保持扁平，复杂条件渲染转为计算属性或方法。
- 列表渲染必须提供稳定 `key`，避免使用索引作为 key（无稳定 id 时除外）。
- 交互态（加载中、空状态、错误态）必须完整，不能只处理成功分支。

## 样式
- 统一使用 `rpx`/平台推荐单位，避免写死不兼容尺寸。
- 避免全局样式污染；局部样式优先，跨页面公共样式放 `uni.scss`。
- 页面结构优先“卡片分区 + 充足留白”，避免密集堆叠与复杂背景图案。
- 文本层级控制在 3 级以内（标题/正文/说明），保证教师高频操作可读性。
- 交互控件保证足够点击热区（建议不小于 `88rpx` 高度）。
