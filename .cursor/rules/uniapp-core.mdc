---
description: uni-app 小程序项目核心规范与交付基线
alwaysApply: true
---

# uni-app Core Guardrails

## 产品上下文
- 产品名称固定为 `乡小育`，面向 `乡村早期教育指导教师`。
- 核心业务围绕 `入户指导`、`亲子活动组织`、`数据记录`、`教学支持` 展开。
- 所有实现优先服务目标：`乡村 0-3 岁婴幼儿家庭教育支持` 的数字化管理与专业化指导。

## 技术基线
- 默认按 `uni-app x + Vue3 + <script setup>` 思路实现；新代码优先使用 TypeScript。
- 保持页面结构清晰：页面放 `pages/`，可复用组件放 `components/`，通用逻辑放 `composables/` 或 `utils/`。
- 公共常量、类型、接口优先抽离，避免散落在页面内。

## 可维护性
- 单个页面不堆砌业务逻辑：数据获取、转换、校验逻辑拆到独立函数或 composable。
- 命名语义化，避免 `data1`、`temp` 之类无语义变量名。
- 优先小步修改，避免无关重构影响已有小程序行为。

## 质量要求
- 变更前后保证可编译，并覆盖主要用户路径（启动、页面跳转、提交操作、异常提示）。
- 新增接口调用必须有失败兜底与用户可理解的提示。
- 禁止提交密钥、token、私有地址等敏感信息。
